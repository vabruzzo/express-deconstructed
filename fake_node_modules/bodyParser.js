const bodyParser = (req, res, next) => {
  req.body = { path: req.path, fake: 1, mock: 2 };
  console.log(`parsed json body: ${JSON.stringify(req.body)}`);
  next();
};

bodyParser.json = () => {};

module.exports = bodyParser;
